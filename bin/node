#!/usr/bin/env sh

if [ "self-update" == "$1" ]; then
    docker pull mkenney/npm
    wget -nv -O $0 https://raw.githubusercontent.com/mkenney/docker-npm/master/bin/node && exit 0
else
    docker run --rm -t -i -v $(pwd):/src:rw mkenney/npm /run-as-user /usr/local/bin/node $@
fi
